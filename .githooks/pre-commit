#!/bin/sh
# Prefer pwsh if available; fall back to Windows powershell.exe
if command -v pwsh >/dev/null 2>&1; then
  exec pwsh -NoProfile -File "$(git rev-parse --show-toplevel)/.githooks/pre-commit.ps1"
else
  exec powershell -NoProfile -ExecutionPolicy Bypass -File "$(git rev-parse --show-toplevel)/.githooks/pre-commit.ps1"
fi
